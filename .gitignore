# Binaries
bin/
dist/
releases/
*.exe
*.exe~
*.dll
*.so
*.dylib
/dtwiz

# Test binary
*.test
__debug_bin*

# Output of the go coverage tool
*.out
coverage.out

# Dependency directories
vendor/
go/

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

!.vscode/launch.json
!.vscode/tasks.json

# OS
.DS_Store
Thumbs.db

# Config files (might contain tokens)
config.yaml

# Local secrets notepad (never commit)
.env
.e2e.env

# Build artifacts
*.log
dtwiz-examples.tar.gz
.integrationtests.env
completions/

# API specs (copied for reference, not tracked)
.api-spec/

# Downloaded collector binary and generated config (contains tokens)
opentelemetry/dynatrace-otel-collector
opentelemetry/config.yaml

# Copilot skills (local-only)
.github/skills/dtwiz/
opentelemetry/*.bak*

# OpenCode tooling
.opencode/package-lock.json
